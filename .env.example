# Generate a strong secret: openssl rand -base64 32
NEXTAUTH_SECRET=xxx
NEXTAUTH_URL=http://localhost:3000

# ---- Google OAuth -------------------------------------------
# Create credentials at: https://console.developers.google.com
# Authorised redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=xxx
GOOGLE_CLIENT_SECRET=xxx

# ---- Email (SMTP) -------------------------------------------
# For local dev: use Mailtrap (https://mailtrap.io) or Ethereal (https://ethereal.email)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=xxx
SMTP_PASSWORD=xxx
EMAIL_FROM=Clipper <noreply@clipper.app>

# ---- Storage (DigitalOcean Spaces) --------------------------
DO_SPACES_KEY=your_spaces_access_key
DO_SPACES_SECRET=your_spaces_secret_key
DO_SPACES_ENDPOINT=https://sgp1.digitaloceanspaces.com
DO_SPACES_BUCKET=your-bucket-name
DO_SPACES_REGION=sgp1
# Optional: CDN endpoint for faster public delivery (recommended for thumbnails/clips)
# DO_SPACES_CDN_ENDPOINT=https://your-bucket-name.sgp1.cdn.digitaloceanspaces.com

# ---- Database (Amazon RDS) ----------------------------------
# Credentials below are read directly by src/lib/db.ts via the PG* env vars.

# ---- App Config ---------------------------------------------
APP_URL=http://localhost:3000
NODE_ENV=development

PGHOST=xxx
PGDATABASE=xxx
PGPORT=5432
PG_USER = xxx
PG_PASSWORD = xxx

# ---- AI Video Pipeline -------------------------------------------------------

# Anthropic Claude (animation spec generation)
# Get your key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Google Gemini (scene planning + script generation + captions)
# Get your key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
# OpenAI key kept for reference (no longer used for pipeline)
OPENAI_API_KEY=sk-...

# Kling AI (image-to-video generation)
# JWT credentials from https://klingai.com/dev/
KLING_API_KEY=xxx
KLING_API_SECRET=xxx

# ElevenLabs (voice conversion / Speech-to-Speech)
# Obtain at https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=xxx
# Voice ID from your ElevenLabs voice library to use for the professional marketing voice
ELEVENLABS_DEFAULT_VOICE_ID=xxx

# RVC voice conversion server (self-hosted, Mac Mini)
# NEXT_PUBLIC_RVC_SERVER_URL — Cloudflare tunnel URL, used by both the browser and the
#   Next.js proxy. Required when the RVC server listens on localhost only (default uvicorn).
#   Run on the Mac Mini: cloudflared tunnel --url http://localhost:8000
#   The URL changes on each tunnel restart — update this value and restart the dev server.
NEXT_PUBLIC_RVC_SERVER_URL=https://<tunnel-subdomain>.trycloudflare.com
# RVC_SERVER_URL — optional LAN IP fallback, only works when the server is bound to 0.0.0.0
#   AND the Next.js host is on the same subnet as the Mac Mini.
RVC_SERVER_URL=http://192.168.1.190:8000
# Voice model name loaded on the RVC server
RVC_DEFAULT_VOICE_MODEL=mind_model

# FFmpeg (video composition + subtitle burn-in)
# Leave blank to use system PATH. Set to absolute path if FFmpeg is not in PATH.
FFMPEG_PATH=
# Temporary directory for FFmpeg intermediate files (must be writable)
FFMPEG_TMP_DIR=/tmp/clipper

# ---- Social Media Publishing -------------------------------------------------

# YouTube Data API v3
# Create OAuth credentials at https://console.cloud.google.com/
# Use the offline refresh-token flow to obtain a long-lived refresh token.
YOUTUBE_CLIENT_ID=xxx
YOUTUBE_CLIENT_SECRET=xxx
YOUTUBE_REFRESH_TOKEN=xxx

# TikTok Content Posting API
# Register your app at https://developers.tiktok.com/
TIKTOK_CLIENT_KEY=xxx
TIKTOK_ACCESS_TOKEN=xxx

# Instagram Graph API (requires Facebook Business Manager)
INSTAGRAM_ACCESS_TOKEN=xxx
INSTAGRAM_PAGE_ID=xxx

# Facebook Graph API (Page video upload)
FACEBOOK_PAGE_TOKEN=xxx
FACEBOOK_PAGE_ID=xxx

# Tvent internal API
TVENT_API_KEY=xxx
TVENT_API_URL=https://api.tvent.app